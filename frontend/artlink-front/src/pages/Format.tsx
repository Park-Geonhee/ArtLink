import React from "react";
// import { BrowserRouter as Router, Switch, Route, Link } from "react-router-dom";

type Props = {
  // props 타입 정의
};

const MyComponent: React.FC<Props> = (props) => {
  // 컴포넌트 구현

  return <div>{/* JSX 내용 */}</div>;
};

export default MyComponent;
